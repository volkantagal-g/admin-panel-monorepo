import { createUseStyles } from 'react-jss';

export default createUseStyles({
  root: {
    '& .ant-select-dropdown': { width: '100%' },
    '& .ant-cascader-menu': { width: '100%' },
  },
  wrapper: { width: '100%' },
});
