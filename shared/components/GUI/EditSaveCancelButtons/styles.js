import { createUseStyles } from 'react-jss';

export default createUseStyles({
  buttonGroup: { paddingTop: '24px' },
  editButton: {
    display: 'flex',
    alignItems: 'center',
    marginLeft: 'auto',
  },
});
