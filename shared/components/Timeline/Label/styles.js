import { createUseStyles } from 'react-jss';

export default createUseStyles({
  label: {
    display: 'flex',
    justifyContent: 'flex-end',
    flexShrink: 0,

    width: '7.1rem',
    height: 'max-content',
    textAlign: 'right',
  },
});
