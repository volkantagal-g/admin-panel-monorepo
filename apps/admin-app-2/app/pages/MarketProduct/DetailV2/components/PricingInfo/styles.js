import { createUseStyles } from 'react-jss';

export default createUseStyles({
  loaderContainer: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    padding: '16px',
  },
});
