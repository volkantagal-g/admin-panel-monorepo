import { createUseStyles } from 'react-jss';

export default createUseStyles({
  selectAllButton: {
    fontSize: '12px',
    whiteSpace: 'nowrap',
    minWidth: '80px',
    textAlign: 'center',
    height: '48px',
  },
});
