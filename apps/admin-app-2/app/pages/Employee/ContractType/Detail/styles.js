import { createUseStyles } from 'react-jss';

export default createUseStyles({
  header: {
    margin: 10,
    borderRadius: 8,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
  goBack: {
    border: '1px solid',
    padding: '5px 10px',
    borderRadius: 2,
  },
});
