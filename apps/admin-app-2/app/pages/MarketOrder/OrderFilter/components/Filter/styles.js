import { createUseStyles } from 'react-jss';

export default createUseStyles({
  fullWidth: { width: '100%' },
  button: { backgroundColor: '#5D3EBD', color: '#ffffff' },
  dateRangePicker: { width: '100%' },
  datePicker: { width: '100%' },
});
