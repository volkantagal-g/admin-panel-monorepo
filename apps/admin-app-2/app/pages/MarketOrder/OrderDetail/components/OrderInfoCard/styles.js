import { createUseStyles } from 'react-jss';

export default createUseStyles({
  orderInfoCard: { height: '11.25rem' },
  orderInfoCardLabel: {
    display: 'flex',
    gap: 8,
    alignItems: 'baseline',
    lineHeight: '0.75rem',
  },
});
