import { createUseStyles } from 'react-jss';

export default createUseStyles(() => {
  return {
    fullWidth: { width: '100%' },
    productsListActionButtonContainer: { marginLeft: 'auto' },
  };
});
