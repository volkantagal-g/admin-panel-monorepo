import { createUseStyles } from 'react-jss';

export default createUseStyles(() => {
  return {
    datePickerWidth: { width: '100%' },
    assignmentStatusTag: {
      height: 32,
      padding: '5px 16px',
      marginBottom: '10px',
    },
  };
});
