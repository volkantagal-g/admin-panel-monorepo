import { createUseStyles } from 'react-jss';

export default createUseStyles({
  importRow: { display: 'flex', gap: 10, alignItems: 'center' },
  buttonContainer: {
    display: 'flex',
    justifyContent: 'flex-end',
    gap: 10,
  },
});
