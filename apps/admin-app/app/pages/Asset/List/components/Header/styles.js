import { createUseStyles } from 'react-jss';

export default createUseStyles({
  btnWrapper: {
    gap: 10,
    display: 'flex',
    flexWrap: 'wrap',
    marginBottom: 10,
  },
  errorMessage: { whiteSpace: 'pre-wrap' },
});
