import { createUseStyles } from 'react-jss';

export default createUseStyles(() => ({
  buttonContainer: { display: 'flex', justifyContent: 'space-between', alignItems: 'center' },
  button: { margin: '10px' },
}));
