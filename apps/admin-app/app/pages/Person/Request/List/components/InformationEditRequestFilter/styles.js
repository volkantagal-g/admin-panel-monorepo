import { createUseStyles } from 'react-jss';

export default createUseStyles(theme => {
  return {
    requestStatusSelect: { width: '100%' },
    filterWrapper: { width: '100%' },
    rangePicker: { width: '100%' },
  };
});
