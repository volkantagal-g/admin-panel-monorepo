import { createUseStyles } from 'react-jss';

export default createUseStyles({
  formButtonsWrapper: { width: '100%', justifyContent: 'end' },
  cancelButton: {
    marginRight: '8px',
    marginLeft: '8px',
  },
});
