import { createUseStyles } from 'react-jss';

export default createUseStyles({
  headerContainer: {
    width: '100%',
    display: 'flex',
    alignItems: 'center',
    marginBottom: 4,
  },
  headerTitle: {
    flex: 1,
    padding: 0,
  },
});
