import { createUseStyles } from 'react-jss';

export default createUseStyles({
  cardHeader: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'flex-end',
  },
  documentHeaderContainer: {
    display: 'flex',
    alignItems: 'center',
  },
  documentHeaderTitle: { flex: '1' },
});
