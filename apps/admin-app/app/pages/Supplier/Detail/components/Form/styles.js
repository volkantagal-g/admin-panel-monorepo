import { createUseStyles } from 'react-jss';

export default createUseStyles({
  title: {
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
    width: '100%',
  },
});
