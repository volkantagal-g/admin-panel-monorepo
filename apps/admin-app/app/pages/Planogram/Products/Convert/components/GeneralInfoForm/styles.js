import { createUseStyles } from 'react-jss';

export default createUseStyles({
  row: { marginBottom: '16px', display: 'flex', alignItems: 'flex-start' },
  selectAllButton: { marginTop: '5px' },
});
