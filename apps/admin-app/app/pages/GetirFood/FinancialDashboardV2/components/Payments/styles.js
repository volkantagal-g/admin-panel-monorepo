import { createUseStyles } from 'react-jss';

export default createUseStyles({
  card: { padding: '0 6px' },
  infoIconClass: { color: '#faad14', marginLeft: 4 },
  alert: { width: 'fit-content', marginBottom: '1rem' },
});
