import Lodable from '@shared/utils/loadable';

export const CancelMarketOrderButton = Lodable(() => {
  return import('./CancelMarketOrderButton');
});
export const ChangeDeliverySlotButton = Lodable(() => {
  return import('./ChangeDeliverySlotButton');
});
