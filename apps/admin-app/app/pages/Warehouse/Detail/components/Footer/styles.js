import { createUseStyles } from 'react-jss';

export default createUseStyles(theme => {
  return {
    formButtonsWrapper: { width: "100%" },
    cancelButton: {
      marginRight: '8px',
      marginLeft: '8px',
    },
  };
});