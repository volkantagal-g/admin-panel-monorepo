import { createUseStyles } from 'react-jss';

export default createUseStyles({
  filterWrapper: { width: '100%' },
  buttonContainer: { display: 'flex', justifyContent: 'flex-end', marginTop: 8 },
  select: { width: '100%' },
});
