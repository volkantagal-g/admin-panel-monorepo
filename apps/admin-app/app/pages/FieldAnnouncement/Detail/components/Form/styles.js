import { createUseStyles } from 'react-jss';

export default createUseStyles(
  { input: { width: '100%' }, inputContainer: { display: 'flex', flexDirection: 'column', marginBottom: 24, gap: 8 } },
);
