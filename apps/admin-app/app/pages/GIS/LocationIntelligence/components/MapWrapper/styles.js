import { createUseStyles } from 'react-jss';

export default createUseStyles({
  loadingIcon: {
    fontSize: '72px',
    color: '#5D3EBC',
    position: 'absolute',
    left: '50%',
    bottom: '50%',
  },
});
