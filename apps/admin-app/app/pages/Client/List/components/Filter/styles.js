import { createUseStyles } from 'react-jss';

export default createUseStyles({
  inputField: {
    width: '100%',
    margin: '2px',
  },
  submitContainer: {
    display: 'flex',
    justifyContent: 'flex-end',
  },
});
