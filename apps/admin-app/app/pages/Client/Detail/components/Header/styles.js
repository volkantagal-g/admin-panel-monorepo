import { createUseStyles } from 'react-jss';

export default createUseStyles({
  tagStatusPrimary: {
    padding: '0 5px',
    color: '#ffffff',
    borderRadius: '4px',
    fontSize: '10px',
    backgroundColor: '#5c3ebd',
  },
});
