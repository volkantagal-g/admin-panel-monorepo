import { createUseStyles } from 'react-jss';

export default createUseStyles({
  activeStatusBGRed: { backgroundColor: '#F2DEDE' },
  noPanelPadding: { '& .ant-collapse-content-box': { padding: 0 } },
});
