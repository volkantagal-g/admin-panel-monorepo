import { createUseStyles } from 'react-jss';

export default createUseStyles(() => {
  return {
    pieChart: {
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      margin: '10px 0',
    },
  };
});
