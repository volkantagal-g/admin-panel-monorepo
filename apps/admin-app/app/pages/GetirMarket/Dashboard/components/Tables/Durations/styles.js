import { createUseStyles } from 'react-jss';

export default createUseStyles({
  container: {
    display: 'flex',
    backgroundColor: '#fff',
    alignItems: 'center',
    margin: '2px 1px',
  },
});
