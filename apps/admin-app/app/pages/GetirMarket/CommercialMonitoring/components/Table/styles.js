import { createUseStyles } from 'react-jss';

export default createUseStyles({
  tableCardWrapper: {
    marginTop: 8,
    '& .ant-card-body': { padding: 8 },
  },
  percentText: { fontSize: '10.5px', fontWeight: 'bold' },
});
