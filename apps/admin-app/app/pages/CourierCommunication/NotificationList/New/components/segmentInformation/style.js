import { createUseStyles } from 'react-jss';

export default createUseStyles(() => {
  return {
    exportButton: { margin: '0% 0% 0% 2%' },
    radioGroup: { width: '100%' },
    radioWrap: { width: '100%' },
  };
});
