import { createUseStyles } from 'react-jss';

export default createUseStyles(theme => ({
  customButton: {
    borderRadius: '50%',
    boxShadow: 'none',
    display: 'flex',
    justifyContent: 'center',
    marginRight: '3px',
  },
}));
