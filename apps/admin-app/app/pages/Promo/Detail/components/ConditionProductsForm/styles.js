import { createUseStyles } from 'react-jss';

export default createUseStyles({
  flexDiv: { display: 'flex', justifyContent: 'space-between' },
  soldOutLabel: { color: 'white', backgroundColor: '#8063c9', padding: '2px 7px', borderRadius: 2 },
});
