import { createUseStyles } from 'react-jss';

export default createUseStyles({
  wrapper: { width: '100%' },
  customConfigSummaryContainer: { marginTop: 24 },
  modalConfirmButton: { marginLeft: 5 },
});
