import { createUseStyles } from 'react-jss';

export default createUseStyles({
  valueFilterActionButtons: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-around',
    alignItems: 'center',
  },
  removeButtonStyle: { marginBottom: 24 },
});
