import { createUseStyles } from 'react-jss';

export default createUseStyles({
  rangePicker: { width: '100%' },
  datePicker: { width: '100%' },
  date: { fontWeight: 600, fontSize: 14 },
  performanceSelect: { width: '100%' },
});
