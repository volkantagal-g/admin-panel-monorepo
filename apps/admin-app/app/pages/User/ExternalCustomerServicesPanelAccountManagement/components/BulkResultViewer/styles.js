import { createUseStyles } from 'react-jss';

export default createUseStyles({
  validationAlert: { textAlign: 'left' },
  validationErrorContainer: {
    maxHeight: '200px',
    overflow: 'auto',
    textAlign: 'left',
    marginTop: '20px',
  },
});
