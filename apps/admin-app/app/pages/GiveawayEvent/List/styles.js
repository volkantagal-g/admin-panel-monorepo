import { createUseStyles } from 'react-jss';

export default createUseStyles(theme => {
  return {
    closeButton: { fontSize: '16px', color: '#f10c0c', marginRight: "20px" },
    redeemButton: { width: '100%' },
  };
});