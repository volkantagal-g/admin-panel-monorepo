import { createUseStyles } from 'react-jss';

export default createUseStyles({
  filterWrapper: { width: '100%' },
  filterSelect: { width: '100%' },
  buttonWrapper: { color: '#fff', backgroundColor: '#5E3EBD' },
  buttonFilter: { marginTop: '18px' },
});
