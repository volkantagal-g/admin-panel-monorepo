import { createUseStyles } from 'react-jss';

export default createUseStyles({
  tableWrapper: {
    width: '100%',
    position: 'absolute',
    right: 0,
    left: 0,
  },
  filterWrapper: { width: '100%' },
});
