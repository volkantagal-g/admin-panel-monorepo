import { createUseStyles } from 'react-jss';

export default createUseStyles(() => {
  return {
    wrapper: {
      fontSize: '14px',
      fontWeight: 400,
      marginBottom: '20px',
    },
    label: { marginBottom: '12px' },
  };
});
