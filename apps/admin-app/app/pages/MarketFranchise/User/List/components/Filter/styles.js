import { createUseStyles } from 'react-jss';

export default createUseStyles(() => {
  return {
    panelContainer: { width: '100%' },
    activenessInput: { width: '100%' },
    buttonContainer: { display: 'flex', height: '100%', alignItems: 'flex-end' },
  };
});
