import { createUseStyles } from 'react-jss';

export default createUseStyles({
  datepickerWrapper: { paddingTop: '8px' },
  numberInput: { width: '100%' },
  alertContainer: { marginBottom: '1rem' },
});
