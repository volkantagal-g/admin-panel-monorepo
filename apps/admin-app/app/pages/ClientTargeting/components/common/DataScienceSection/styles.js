import { createUseStyles } from 'react-jss';

export default createUseStyles({
  container: { 
    margin: '.5rem 0',
    width: '100%',
  },
  fullWidth: { width: '100%' },
  formItemWrapper: { border: '0' },
});